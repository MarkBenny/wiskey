<%= form_for(@comment, :url => post_comment_path(@comment.post_id, @comment)) do |f| %>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
